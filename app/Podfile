platform :ios, '10.0'
inhibit_all_warnings!

target 'SAM-Swift' do
  use_frameworks!

  pod "Kanna", :git => "https://github.com/tid-kijyun/Kanna", :branch => "feature/v4.0.0"
  pod "FLAnimatedImage", "1.0.12"
  
  target 'SAM-SwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SAM-SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
